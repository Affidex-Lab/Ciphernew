Summary
- Add onboarding wizard and route (/onboarding) with steps: generate/import owner key, predict address + AA deploy, set guardians (2-of-3) with delay, backup Recovery Kit (PBKDF2+AES-GCM) and optional Passkey kit, finish to Dashboard.
- Implement lightweight analytics without SDK using PostHog capture API. Env: VITE_POSTHOG_KEY, optional VITE_POSTHOG_HOST. Events: onboarding_started, onboarding_completed (accountAddr, networkKey), tx_sent (type/symbol/usd).
- Optional Supabase upsert client (no SDK) using REST if VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are present. Upserts { account_addr, email, created_at } into 'users' table.
- Add Send dialog supporting native and ERC-20 transfers using existing EIP-4337 utils: estimate → sponsor → sign → send; poll receipt and update history. Keep “Disposable transfer” as an advanced separate flow.
- UX polish: Landing CTA routes to /onboarding; /access validates private key and previews derived owner address; toasts used for error/info. Config reads from public/config.json and can be overridden via localStorage.

Notes
- No server-side secret storage; analytics are anonymous by default.
- New files: src/components/SendDialog.tsx, src/lib/analytics.ts, src/lib/supabase.ts.
- No new UI libraries added.

Testing
- Ensure VITE_POSTHOG_KEY is set to see analytics events.
- Optional: create Supabase table 'users' with column account_addr (PK), email (nullable), created_at (timestamp), enable REST; set env vars to upsert.


₍ᐢ•(ܫ)•ᐢ₎ Generated by [Capy](https://capy.ai) ([view task](https://capy.ai/project/295ccaf2-4e8d-47f1-b6a4-00e5e19f4ac2/task/cfe410b7-8bc5-47c3-ba2b-3685b0ca7e61))