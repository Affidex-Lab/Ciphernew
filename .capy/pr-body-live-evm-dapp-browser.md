## Summary
- Add live EVM data refresh:
  - ETH balance auto-refreshes every ~20s with in-flight guard + cleanup
  - ERC-20 balances auto-refresh every ~35s via existing refreshTokens() with guard + cleanup
  - USD price auto-refreshes every ~60s based on active network assetId (e.g., ethereum)
- Improve WalletConnect flow:
  - Auto-create Disposable Key in-memory when opening Connect dApp (if none present)
  - Dialog adds actions: Open dApp Browser (popup /browser) and Pair from clipboard
  - Manual paste remains as fallback; inline notice informs that the temporary key is auto-destroyed on disconnect
  - Dashboard listens for postMessage({ type: 'wc-uri', uri }) from popup and pairs
- New /browser route and page providing a simple in-app browser with address bar + iframe, Pair from clipboard (posts wc-uri to opener), and Copy current URL
- Bottom nav Browser button opens the /browser popup

## Why
- Provide “live” tokens and price without manual refresh to improve UX and keep balances accurate
- Streamline dApp connection by bundling discovery/browsing and pairing in a guided popup, and ensure a temporary session key is always available

## Files
- src/App.tsx — intervals, WC dialog enhancements, postMessage listener, bottom nav Browser handler
- src/AppRouter.tsx — adds /browser route
- src/pages/Browser.tsx — new in-app browser page

## Notes
- Disposable key is only in memory and is destroyed on disconnect; not persisted
- Intervals skip if a prior call is in-flight and clean up on unmount to avoid RPC hammering
- postMessage origin restricted to same-origin

## Testing
1. Open Dashboard on EVM with an account
2. Observe ETH balance updates ~20s, ERC-20s ~35s, USD price ~60s
3. Click Connect dApp → confirm dialog actions present and disposable key ensured
4. Click Open dApp Browser → navigate to a dApp; copy/pair WC URI via clipboard or postMessage
5. Approve proposal with disposable key; Disconnect should destroy key automatically


₍ᐢ•(ܫ)•ᐢ₎ Generated by [Capy](https://capy.ai) ([view task](https://capy.ai/project/c69eefa1-f7f6-4442-8c16-0e628155bf9f/task/50d9c85a-07e8-4ffc-840b-1294f055ccf3))